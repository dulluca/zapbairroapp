org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Desativa o cache incremental do Kotlin. Necessario quando o projeto e o pub
# cache (C:) ficam em drives diferentes (ex.: projeto em D: nesta maquina, ou no
# Google Drive G: em outra): a compilacao incremental tenta calcular um caminho
# relativo entre drives distintos e quebra no Windows ("different roots").
kotlin.incremental=false
